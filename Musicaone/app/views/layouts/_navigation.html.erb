<!-- Static navbar -->
<nav class="navbar navbar-default navbar-static-top">
    <div class="container">
        <div class="navbar-brand">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                <span class="sr-only">Musicalink.fr</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            <%= link_to image_tag("http://musicalink.fr/mu_logo.png"), root_path %>
        </div>
        <div id="navbar" class="navbar-collapse collapse">
            <ul class="nav navbar-nav navbar-right">

                <% if (!user_signed_in?) %>
                <li>
                    <%= link_to new_user_registration_path do %>
                    <i class="fa fa-user-plus" aria-hidden="true"></i> Inscription
                    <% end %>
                </li>
                <li>
                    <%= link_to new_user_session_path do %>
                    <i class="fa fa-user" aria-hidden="true"></i> Connexion
                    <% end %>
                </li>
                <% else %>
                <li>
                    <%= link_to new_prof_path do %>
                    <i class="fa fa-home fa-lg"></i>Devenez prof !
                    <% end %>
                </li>
                <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%= image_tag avatar_url(current_user), class: "img-circle avatar-small" %>   <%= current_user.fullname %> <span class="caret"></span></a>
                    <ul class="dropdown-menu">
                        <li>
                            <p>
                                <%= link_to root_path do %>
                                <i class="fa fa-headphones" aria-hidden="true"></i> Mes Leçons
                                <% end %>
                            </p>
                        </li>

                        <li>
                            <p>
                                <%= link_to edit_user_registration_path do %>
                                <i class="fa fa-cog" aria-hidden="true"></i> Modifier mon profil
                                <%end %>
                            </p>
                        </li>
                        <li role="separator" class="divider"></li>
                        <li>
                            <p>
                                <%= link_to destroy_user_session_path, method: :delete do %>
                                <i class="fa fa-user-times" aria-hidden="true"></i> Déconnexion
                                <% end %>
                            </p>
                        </li>
                    </ul>
                </li>
                <% end %>
            </ul>
        </div><!--/.nav-collapse -->
    </div>
</nav>